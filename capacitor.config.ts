import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.goldmasteregypt.app',
  appName: 'Gold Master Egypt',
  webDir: 'dist'
};

export default config;
